# ========================================
# KRONØS SYNC - Environment Variables
# ========================================

# NextAuth (CRÍTICO)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-here-change-this-in-production

# Google OAuth (CRÍTICO)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Database - Neon/Vercel Postgres (CRÍTICO)
# Copie essas variáveis do seu painel Vercel ou Neon Dashboard
POSTGRES_PRISMA_URL="postgresql://user:password@host/database?sslmode=require&pgbouncer=true"
POSTGRES_URL_NON_POOLING="postgresql://user:password@host/database?sslmode=require"
DATABASE_URL="postgresql://user:password@host/database?sslmode=require"

# Email - Resend (CRÍTICO)
RESEND_API_KEY=re_your_api_key_here
RESEND_FROM_EMAIL="KRONOS SYNC <acesso@kronosync.com.br>"

# Segurança (OPCIONAL - tem fallback)
DATA_ENCRYPTION_KEY=your-32-character-encryption-key

# Convites Especiais (OPCIONAL)
KRONOS_TEAM_KEY=your-team-invite-key

# ========================================
# INSTRUÇÕES DE CONFIGURAÇÃO
# ========================================
#
# 1. DESENVOLVIMENTO LOCAL:
#    - Copie este arquivo para .env.local
#    - Substitua os valores acima pelas suas credenciais reais
#
# 2. PRODUÇÃO (VERCEL):
#    - Vá em: Settings > Environment Variables
#    - Adicione cada variável marcada como CRÍTICO
#    - NEXTAUTH_URL deve apontar para seu domínio (ex: https://kronos.app)
#    - NEXTAUTH_SECRET deve ser único (gere com: openssl rand -base64 32)
#
# 3. BANCO DE DADOS (NEON):
#    - Acesse: Dashboard > Connection String
#    - Copie a string de conexão com pooling para POSTGRES_PRISMA_URL
#    - Copie a string sem pooling para POSTGRES_URL_NON_POOLING
#
# 4. GOOGLE OAUTH:
#    - Acesse: https://console.cloud.google.com
#    - Crie um projeto e ative Google Calendar API
#    - Em "Credenciais", crie OAuth 2.0 Client ID
#    - Adicione redirect URI: https://seu-dominio.com/api/auth/callback/google
#
# 5. RESEND (EMAIL):
#    - Acesse: https://resend.com/api-keys
#    - Crie uma API key
#    - Configure o domínio de envio verificado
#
